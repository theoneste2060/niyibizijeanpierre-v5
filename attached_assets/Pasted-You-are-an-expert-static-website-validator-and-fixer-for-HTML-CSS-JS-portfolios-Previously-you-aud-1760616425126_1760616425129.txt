You are an expert static website validator and fixer for HTML/CSS/JS portfolios. Previously, you audited and fixed issues in NIYIBIZI Jean Pierre's site (geotechnical engineer/AI innovator portfolio). Now, perform a follow-up audit on the updated files: Focus on remaining structural errors, content gaps, and validation. Output clean, production-ready files with a changelog.

### Updated Project Context
- Static HTML site: index.html, about.html (if added), projects.html, publications.html, blog.html, blog-ai-geotech.html, blog-sustainable-construction.html, blog-ai-driven-soil-analysis.html, media.html, contact.html (if present), style.css, navbar.js.
- Recent Fixes Assumed: Polished metas/links/alts; JS error handling for views.
- Goals: 100% W3C-valid HTML, no parsing errors, complete content. No new features—just fixes.

### Specific Issues to Target (From Recent Scan)
1. **HTML Structure/Validation:**
   - Fix unclosed/malformed tags in: index.html, projects.html, media.html, blog-ai-geotech.html, publications.html, blog-sustainable-construction.html, blog.html (e.g., ensure all <div>, <p>, <ul> close properly; self-closing for <img>/<hr>).
   - Validate full HTML5 (simulate W3C): No deprecated elements, proper nesting (e.g., <figure> with <figcaption> in blogs).

2. **Content Completeness:**
   - blog-sustainable-construction.html: Complete truncated UL in "Understanding Climate Challenges" section—full item: "<li>Soil erosion accelerating degradation and shortening infrastructure lifespan.</li>".
   - All blogs: Add "Related Posts" section in article-footer (e.g., in soil-analysis: link to ai-geotech.html as "<a href='blog-ai-geotech'>Explore AI in Geotech</a>").

3. **Links & Navigation:**
   - publications.html: Fix broken DOI link (href="#")—replace with placeholder like "https://doi.org/10.xxxxxxx" or remove if stub.
   - Global: Ensure all navbar <a> have aria-label; test relative paths (e.g., ./assets/img/ for images).

4. **JS/CSS Polish:**
   - navbar.js: Add console.log for toggle success; ensure no leaks on mobile.
   - style.css: Check for unused selectors (e.g., .btn-p... truncation in original—complete if present); ensure .blog-grid responsive (media query for <768px stacks cards).
   - blog.html: Enhance view counter JS—add localStorage persistence for offline testing (default 0 if fetch fails).

5. **SEO/Accessibility Extras:**
   - Add <meta name="updated" content="2025-10-16"> to all <head> for freshness.
   - Images: Double-check alts (e.g., add to any new figs); lazy-load all <img> with loading="lazy".
   - Generate/Update: sitemap.xml (include all pages/posts with <lastmod>2025-10-16</lastmod>); add robots.txt (Allow: /).

### Workflow
1. Scan all files with a validator sim (e.g., BeautifulSoup/HTML lint).
2. Apply fixes in-place—preserve content/design.
3. Test: Simulate browser open (no errors); mobile view (grids stack).
4. Output: Changelog (e.g., "Fixed unclosed <div> in index.html"), full updated files, and validation summary (e.g., "All HTML valid").
5. Flag: Any non-fixable issues (e.g., missing assets).

Run the audit/fixes now—make it bulletproof for Netlify deploy.